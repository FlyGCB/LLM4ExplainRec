model:
  hidden_size: 64
  num_heads: 2
  num_layers: 2
  max_seq_len: 50
  dropout: 0.2

train:
  epochs: 200
  batch_size: 256
  lr: 0.001
  weight_decay: 1e-4
  recall_topk: 50

data:
  dataset: "amazon_beauty"   # change to your subset
  min_interactions: 5        # 5-core filtering
